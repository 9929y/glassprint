# Glassprint —— 会显影出一张画的玻璃俄罗斯方块

v4 · 2026-08-29 · 明色。完整界面，单文件零依赖。
Artifact：https://claude.ai/code/artifact/ae404b1a-e2f9-4507-bc78-1f759649466e
文件：`index.html`（仓库 `~/Developer/glassprint`，原名 fuse-board.html）

---

## 核心机制

**一张画被切成井的 10 × 20 = 200 格。** 画默认是一张柔和渐变（八张配方随机抽），也可以是**你拖进井里的任何一张照片**。

- 方块下落时是**透明玻璃，什么都不显示**
- **一落地，颜色从每一格中心晕开** —— 显示的正是那张画在那个坐标上的那一小块
- 新落的格子先是**未显影**的（低饱和、偏亮），2.6s 缓到实色。整个盘面最近落的那几块永远在微微「显影中」
- 堆叠 = 画在显影
- **消行之后，上面所有方块下移并重新上色**（它们现在压在画的不同位置上）。已显影的格子交叉淡入，不会退回未显影

**硬约束：游戏结束前，画只出现在已经落地的格子上。** 底下不铺任何画层，ghost（落点细框）也不透色，只给位置。屏幕上只有画是彩色的，房间是纸。

## 顺序是硬要求

格子必须**严格有序**（由深到浅），否则拼不成一张画。配方是有方向的线性 ramp（160°–214°，四个色标单调递增），采样是画到 6 倍分辩率再每 6×6 求平均降到 10×20 —— 一格拿到的是它那一片的**真实平均色**。**不做任何按方块类型的偏移。** v2 做过，它恰好破坏顺序。格子只显示画，不显示它自己的想法。

照片走同一条采样：cover-crop 到 1:2，`drawImage`，然后同一段平均循环。**不做影调校正**，太暗的照片就是暗的。

## 落地上色

| 层 | 做法 |
|---|---|
| 颜色 | 内层 `<i>` 用 `clip-path: circle(0% → 80%)` 从中心晕开，420ms |
| 显影 | `<i>` 从 `saturate(.22) brightness(1.2)` 过渡到 `none`，2.6s |
| 挤压 | `<i>` 跑 `scaleY(.84 → 1.04 → 1)`，460ms，原点在底边 |
| 闪光 | `.tile::before` 一层**固定** 22px 白色光晕，只动 `opacity`（合成层，不重绘） |
| 错峰 | 一块的四格按阅读顺序 0 / 55 / 110 / 165ms |
| 换色 | 塌落后 `--c` 变化，`transition: background .52s` 交叉淡入 |

**所有错峰都是 CSS delay，不是 `setTimeout`。** 标签页被节流时只是慢，不会漏掉上色。

## 流畅度

- **落定瓷砖零 DOM 写入。** 每个 block 带 `moving` 标记，只在弹簧还没收敛时写 `transform`。稳态下 `frame()` 里那段循环一次都不写（MutationObserver 实测 0）。
- **DAS 自己做。** 按住方向键：170ms 后开始连发，每 40ms 一格，按**帧时间累积**算而不是系统 key-repeat（各系统不一致且慢）。方向键的 `e.repeat` 直接忽略。实测按住 400ms 走了 3 格到墙。
- 连续重力、渲染弹簧、旋转缓入照旧。基础重力 1.25 cells/s，随消行数缓慢爬升。

## 界面

只有**一个井**，做成**凹进纸面的托盘**（顶部内阴影、底边一条亮线），不是浮着的卡片。底下一排图形按键：

`← → ↑ ↓ ␣` · 照片（相框 + 太阳；已载入时一道斜杠）· 存（箭头进托盘，盘面空时灰掉）· 播放/暂停（**停着显示三角、跑着显示双竖杠**：显示你能做的那个动作）· 重开

无文字、无数字、无 hold、无 next。瓷砖高光换成左上角一小片锐利镜面反光 + 顶边 1px 亮线，画的颜色不再被一大片白渐变洗淡。

## 照片

拖任何图片进井 / 粘贴 / 点相框按钮选文件 → 立刻成为画：field 重新采样，**所有已落地的格子立刻重新上色**成照片对应位置的颜色。再点相框 = 移除照片、回到渐变。有照片时，重开和顶到头换画都**保留照片**。

## 结束 = 熨烫，不是失败画面

顶到头：玻璃壳褪掉，格子涨大 `cell/tile` 倍互相合拢成一整块画，一道慢高光扫过，**当前画面被画成一张 600×1200 的 PNG 叠在井上**（可右键存），停 2.8s，整块抬起下沉消失，换一张新画。无字、无弹窗。

「存」按钮任何时候都能把当前盘面存成同样规格的 PNG（纸白底、保留缝和圆角）。**在 artifact 里走 `downloads` 能力**（`claude.use('downloads')` → `save({filename, data})`，viewer 弹一次确认）；本地双击打开时没有 `window.claude`，自动退回普通 `<a download>`。两条路拒绝时都不吭声 —— 这个界面唯一的字就是那个按钮。

## 修过的四个真 bug

1. **暂停按钮从来没暂停过。** 点按钮 → 按钮抢焦点 → 井 `blur` → `halt()`（paused=true）→ 随后 click → `toggle()` 看见已 paused → 又 `play()`。修：`.app` 的 `pointerdown` 统一 `preventDefault`（房间里只有井能拿焦点），blur 处理器再对 `relatedTarget` 是图标的情况放行。
2. **暂停图标语义反了。** 已交换。
3. **方块叠加。** `measure()` 在 resize 时只改了瓷砖尺寸，没重算下落块的四格位置（artifact 的 iframe 加载后立刻 resize 一次，正好触发）。修：重建整块，落定块重新安放并把弹簧归零。
4. **键盘焦点。** 挂在 `window` 上但没人拿焦点 → 任意按键、任意点击都会 claim 焦点并开局。

## 走过的弯路（都记着）

1. v1 把颜色放在右边一块拼豆板上 → 画和方块脱节，界面多一块看不懂的东西。
2. v2 把渐变做成房间背景 → 参考图是画本身该长什么样，不是背景色。
3. v2 给每种方块加微偏移 → 破坏「拼成一张画」的前提。**保证顺序 > 参与感。**
4. v3 用 `setTimeout` 错峰上色 → 节流时格子留白。改 CSS delay。
5. v4 第一版 DAS 用绝对时钟 → 与帧时间源不一致时失效。改 dt 累积。

## 还可以做

- 完整 SRS wall kick（含 I 块偏移）。现在是五档 nudge。
- 音效：熨烫那一下最值得有声音。最多两个 cue、默认静音。
- 移动端触控是「能用」不是「好用」。
- 一组内置照片（data URI 会让单文件变大，所以现在没放）。

## 与页脚那版的关系

`footer-under-glass.html`（仍在库里 `01_projects/vibecoding/glass-tetris/`）是**页脚组件**：10×12、自己会玩、无色、无全屏。
`index.html` 是**完整游戏界面**：明色、10×20 的井就是画布、可用自己的照片。
两者共用 Figma `190:493` 的材质规格（圆角 = 边长 10%、内容裁切、半透白；明色下 alpha 提到 .44/.72）和同一套动效原则（逻辑吸附 / 渲染弹簧 / 连续重力）。
